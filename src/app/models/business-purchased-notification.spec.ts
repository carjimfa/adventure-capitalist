import { BusinessPurchasedNotification } from './business-purchased-notification';

describe('BusinessPurchasedNotification', () => {
  it('should create an instance', () => {
    expect(new BusinessPurchasedNotification()).toBeTruthy();
  });
});
